interface:
  display_name: "HMM Task And Concurrency"
  short_description: "任务和并发护栏"
  default_prompt: "当 HMM 工作触及 TaskManager、long-running tasks、cancellation、progress events、locks、queues 或 concurrent file/database operations 时，使用 $hmm-task-and-concurrency。"

policy:
  allow_implicit_invocation: true
