interface:
  display_name: "HMM Task And Concurrency"
  short_description: "Task and concurrency guardrails"
  default_prompt: "Use $hmm-task-and-concurrency when HMM work touches TaskManager, long-running tasks, cancellation, progress events, locks, queues, or concurrent file/database operations."

policy:
  allow_implicit_invocation: true
