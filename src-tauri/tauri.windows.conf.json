{
  "build": {
    "beforeDevCommand": "corepack pnpm run prepare:windows-sidecars:dev && corepack pnpm dev",
    "beforeBuildCommand": "corepack pnpm build && corepack pnpm run prepare:windows-sidecars"
  },
  "bundle": {
    "externalBin": [
      "binaries/hmm-save-backup-worker",
      "binaries/hmm-save-backup-installer-cleanup"
    ],
    "windows": {
      "nsis": {
        "installerHooks": "windows/nsis-installer-hooks.nsh"
      },
      "wix": {
        "template": "windows/wix/main.wxs",
        "fragmentPaths": ["windows/wix/installer-cleanup.wxs"]
      }
    }
  }
}
