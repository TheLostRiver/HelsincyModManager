## 改动摘要
- 修复了紧凑列表视图 (List View) 在大分辨率下自动换行过多的 Bug，现在强制要求一行排列 2 个卡片。
- 修复了机能面板视图 (Tech View) 样式与设计稿 `toolbar_demo.html` 不一致的问题：
  - 为 Tech 视图的卡片补充了实色边框。
  - 修复了由于 `data-status` 属性未绑定到卡片容器导致的指示灯颜色丢失问题，实现了根据状态切换绿、红、橙色。
  - 补充了原设计稿中作者、尺寸等字段的具体 CSS 样式。

## 参与 Agent
- 主控：Codex / Antigravity
- Worker：Antigravity
- 审查：Helsincy

## 任务包范围
- 允许修改文件：
  - `src/features/mods/ModLibraryPage.css`
  - `src/features/mods/ModPosterCard.tsx`
- 实际修改文件：
  - `src/features/mods/ModLibraryPage.css`
  - `src/features/mods/ModPosterCard.tsx`
  - *(可能包含之前的其他 UI 重构记录)*

## 验证
- [x] cmd /c corepack pnpm run typecheck
- [x] cmd /c corepack pnpm run lint
- [x] cmd /c corepack pnpm run build
- [x] powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1

## 视觉证据
- 截图：*(由用户本地确认)*
- 浏览器 smoke test：*(由用户本地确认)*

## 风险和未覆盖项
- 由于强行要求了 List 视图两列排布，若后续引入了极端窄屏场景（如低于 800px 宽度），可能需要额外的媒体查询做折叠处理。
