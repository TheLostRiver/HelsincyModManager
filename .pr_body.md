## 改动摘要
- 优化了 Mod 卡片在被选中状态（is-selected）下的视觉反馈设计。
- 移除了原先会导致大面积深色文字难以阅读的实色蓝底背景（`var(--color-primary-action-bg)`），将其替换为卡片默认表面底色（`var(--color-surface)`）。
- 强化了卡片的主题色边框描边，并增加微弱的主题色高光阴影，配合原有的蓝色勾选指示器，实现了更现代、更清晰的选中态指示。

## 参与 Agent
- 主控：Codex / Antigravity
- Worker：Antigravity
- 审查：Helsincy

## 任务包范围
- 允许修改文件：
  - `src/features/mods/ModLibraryPage.css`
- 实际修改文件：
  - `src/features/mods/ModLibraryPage.css`

## 验证
- [x] cmd /c corepack pnpm run typecheck
- [x] cmd /c corepack pnpm run lint
- [x] cmd /c corepack pnpm run build
- [x] powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1

## 视觉证据
- 截图：*(由用户本地确认)*
- 浏览器 smoke test：*(由用户本地确认)*

## 风险和未覆盖项
- 此改动仅涉及纯 CSS 渲染，不涉及 DOM 层级的逻辑破坏，无新增技术风险。
