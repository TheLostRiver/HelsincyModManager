## 改动摘要
- 优化了 Mod 库顶部工具栏的布局与交互体验。
- 将原本挤在同一行的“搜索栏”与“分类标签”拆分为上下两行：
  - **上排**：展示加宽的搜索框以及最右侧的排版视图切换按钮，避免用户将分类标签误认为搜索按钮，也给搜索框提供了更充足的输入空间。
  - **下排**：独立放置 Mod 分类筛选标签，确保未来扩展更多分类时标签能自然换行排布，不干扰核心搜索区域。

## 参与 Agent
- 主控：Codex / Antigravity
- Worker：Antigravity
- 审查：Helsincy

## 任务包范围
- 允许修改文件：
  - `src/features/mods/LibraryToolbar.tsx`
  - `src/features/mods/ModLibraryPage.css`
- 实际修改文件：
  - `src/features/mods/LibraryToolbar.tsx`
  - `src/features/mods/ModLibraryPage.css`

## 验证
- [x] cmd /c corepack pnpm run typecheck
- [x] cmd /c corepack pnpm run lint
- [x] cmd /c corepack pnpm run build
- [x] powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1

## 视觉证据
- 截图：*(由用户本地确认)*
- 浏览器 smoke test：*(由用户本地确认)*

## 风险和未覆盖项
- 由于增加了工具栏的垂直高度，略微占据了少量下方卡片列表的显示空间，但换取了更好的操作清晰度和未来扩展性。
