## 改动摘要
修复了 `ModLibraryPage` 中第一排卡片在选中状态下，由于 `translateY(-3px)` 及外发光阴影向上溢出，进而被滚动容器顶部边缘裁切（出现平头线框）的问题。
解决方式：为卡片网格容器 `.mod-grid` 增加了 `padding: 8px 0;`，为卡片上方的选中特效留出了安全的视觉空间。

## 参与 Agent
- 主控：Antigravity (Gemini 3.1 Pro)
- Worker：Antigravity (Gemini 3.1 Pro)
- 审查：N/A

## 任务包范围
- 允许修改文件：`src/features/mods/ModLibraryPage.css`
- 实际修改文件：同上

## 验证
- [x] cmd /c corepack pnpm run typecheck
- [x] cmd /c corepack pnpm run lint
- [x] cmd /c corepack pnpm run build
- [x] powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1

## 视觉证据
- 截图：无（纯 CSS padding 调整修复裁切 bug）
- 浏览器 smoke test：验证选中卡片时顶部外发光完整。

## 风险和未覆盖项
无。
